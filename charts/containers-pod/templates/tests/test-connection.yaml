apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "containers-pod.fullname" . }}-test-connection"
  labels:
    {{- include "containers-pod.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "containers-pod.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
